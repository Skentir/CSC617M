bpM(150)
instrument(Piano)
sample_note = quarter(G, 3)

staff (4,4) {
    measureUp {
        slurstart
        chord(quarter(C, 3), quarter(C, 5))
        slurstart
        quarter(C, 3)
        sample_note
        sample_note
    }
    measureDown {
        quarter(A, 3)*
        slurend
        eighth(A, 3)
        half(rest)
        slurend
    }
   
}