bpm(300)
sample_note = quarter(A, 2)
sample_kirs = quarter(A, 2)
sampleChord2 = chord(half(A, 2), half(B, 4), half(C,5))

staff (4,4) {
    measure {
        quarter(A, 2)
        quarter(B, 2)
        quarter(C, 2)
        quarter(D, 2)
    }
}
