bpM(150)
instrument(Piano)
sample_note = quarter(G, 3)

staff (4,4) {
    measureUp {
        slurstart
        quarter(C, 3)
        slurstart
        quarter(C, 3)
        sample_note
        sample_note
    }
    measureDown {
        quarter(A, 3)*
        slurend
        eighth(A, 3)
        half(rest)
        slurend
    }
   
}