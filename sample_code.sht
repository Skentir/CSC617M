bpm(300)
sample_note = quarter(A, 2)
sample_kirs = quarter(A, 2)
sampleChord2 = chord(double(A, 2), half(B, 4), double(C,5))



staff (3,8) {
    measure {
        sample_note
        half(C, 6)
        half(G, 2)
        sampleChord2
    }
}

