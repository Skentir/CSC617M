bpm(300)
sample_note = quarter(A, 2)
sample_kirs = full(A, 2)
sample_kirs1 = eighth(A, 2)
sample_kirs2 = double(A, 2)
sample_kirs3 = half(A, 2)
sampleChord2 = chord(double(A, 2), half(B, 4), quarter(C,5))


staff (4,4) {
    measure {
        quarter(A, 2)
        quarter(B, 2)
        quarter(C, 2)
        quarter(D, 2)
    }
}