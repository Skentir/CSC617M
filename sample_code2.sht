bpm(300)
instrument(ACoustic GuitaR)
staff(3, 4) {

	accidental( #A, _B, C)

	measure {
		accidental( _A, #B, C)
		continuous {
			quarter(A, 2)
			chord(half(A, 2), half(B, 2))
			quarter(B, 2)
		}
		accidental(A, B, F)
		quarter(C, 0)
		half(B, 3)
	}
	

}
