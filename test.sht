bpm(300)
instrument(Piano)
sample_note = QuArTeR(A, 2)
sample_kirs = quarter(A, 2)
sampleChord2 = chord(half(A, 2), half(B, 4), half(C,5))

staff (4,4) {
    measure {
        sample_kirs
        quarter(B, 2)
        chord(quarter(A, 2), quarter(B, 4), quarter(C, 5))
        quarter(D, 2)
    }
    measure {
        quarter(A, 2)
        quarter(B, 2)
        quarter(C, 2)
        quarter(D, 2)
        quarter(D, 2)
    }
}

melody chorus {
    staff (3,4) {
        measure {
            quarter(C, 2)
            quarter(D, 2)
            continuous {
                quarter(C, 2)
                quarter(D, 2)
            }
        }
    }
}